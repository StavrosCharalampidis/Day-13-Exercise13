# import from sys the argv module
from sys import argv

# Those args input from terminal when you run the script
script, x, y, z = argv

# And print the outputs
print(f'Those Variables is type when you run the script {script}, {x} {y} {z}')
